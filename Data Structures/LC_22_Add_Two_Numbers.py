"""
You have two linked lists. Each list represents a number, but the digits are written backward. Your job is to add the two numbers together and return the result as a new linked list, also written backward. Don't worry about extra zeros at the start of the numbers, except if the number is just 0.
"""

# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
		while next != None:
			l1.val = l1.val